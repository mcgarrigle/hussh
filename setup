#!/usr/bin/bash

# --------------------------------------

function setup_ca {
  mkdir -p "${HERE}/ca" "${HERE}/keys" "${HERE}/profiles" "${HERE}/logs"

  openssl rand -base64 32 > "${HERE}/keys/aes.key"

  ssh-keygen -t rsa -f "${HERE}/keys/host.rsa"

  ssh-keygen -t ecdsa -f ${HOST_CA} \
    -C "Host Certficate Authority for *.${HUSSHD_DOMAIN}"
  ssh-keygen -t ed25519 -f ${USER_CA} \
    -C "User Certificate Authority for *.${HUSSHD_DOMAIN}"
}

function setup_install {
  python -m venv .venv
  source ${HERE}/.venv/bin/activate
  pip3 install paramiko pyaml sshkey-tools
}

function setup_run {
	echo "---------------------------"
	echo "// husshd"
	echo "---------------------------"
  echo "domain: ${HUSSHD_DOMAIN}"
  echo "port:   ${HUSSHD_PORT}"
	echo
  source ${HERE}/.venv/bin/activate
  ./husshd
}

# --------------------------------------
#

THIS=$(realpath "$0")
HERE=$(dirname "${THIS}")


HOST_CA="${HERE}/ca/ssh_ca_host_key"
USER_CA="${HERE}/ca/ssh_ca_user_key"

DOMAIN=$(hostname --domain)
HUSSHD_DOMAIN="${HUSSHD_DOMAIN:-${DOMAIN}}"
HUSSHD_PORT="${HUSSHD_PORT:-5555}"

COMMAND="$1"
shift
setup_${COMMAND} $@
